# Email Configuration for hConnect Firebase Functions
# Copy this file to .env and fill in your values
# IMPORTANT: Do NOT commit .env to version control!

# ============================================
# OPTION 1: Resend (RECOMMENDED - easiest)
# ============================================
# Get a free API key at https://resend.com
# Free tier: 3000 emails/month, 100 emails/day
RESEND_API_KEY=re_xxxxxxxxxxxx

# Optional: Custom from address (requires verified domain on Resend)
# Without this, emails come from onboarding@resend.dev (works for testing)
# RESEND_FROM=hConnect <alerts@yourdomain.com>

# ============================================
# OPTION 2: Gmail SMTP (more complex setup)
# ============================================
# EMAIL_FROM=alerts@yourdomain.com
# EMAIL_SMTP_HOST=smtp.gmail.com
# EMAIL_SMTP_PORT=465
# EMAIL_SMTP_USER=your-email@gmail.com
# EMAIL_SMTP_PASS=your-app-password (from https://myaccount.google.com/apppasswords)
# EMAIL_SMTP_SECURE=true

# ============================================
# App Settings
# ============================================
# Your app's base URL for deep links in emails
APP_BASE_URL=https://hconnect-6212b.web.app

# ============================================
# Slack Integration (Optional)
# ============================================
# Create a Slack app at https://api.slack.com/apps
# These are set as Firebase secrets, NOT environment variables:
#   firebase functions:secrets:set SLACK_CLIENT_ID
#   firebase functions:secrets:set SLACK_CLIENT_SECRET
#   firebase functions:secrets:set SLACK_SIGNING_SECRET
#
# Get these values from your Slack App:
# - Client ID & Secret: App Settings > Basic Information
# - Signing Secret: App Settings > Basic Information > App Credentials
